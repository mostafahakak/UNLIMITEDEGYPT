# Render Blueprint: Dashboard → New → Blueprint → deploy from this repo
# Docs: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: unlimited-egypt-paymob-api
    runtime: node
    region: frankfurt
    branch: main
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
